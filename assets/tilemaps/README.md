# Tilemap Placeholders

Session 05 では、開始村と塔前荒地の field foundation をコード描画の placeholder で実装している。

- 実動 scene: `scenes/field/field_root.tscn`
- shared layout constants: `scripts/world/starting_village_layout.gd`
- smoke: `python3 tools/qa/field_smoke.py`

今後 Tiled / TileSet ベースへ移行する際も、導線と座標の正は
`docs/specs/worlds/01_starting_village_layout.md` を先に参照する。
